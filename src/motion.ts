/**
 * How much every pen line shimmers, in sketch units.
 *
 * drawably's default is 0.3, which is barely perceptible. This is the single
 * knob for the whole page -- every control and custom sketch reads it, and the
 * cycle speed it pairs with lives in styles.css (search drawably-svg).
 */
export const BOIL = 0.9;
